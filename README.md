# 🧮 Лабораторная работа №4 — Численные методы решения нелинейных уравнений (C++)

## 📘 Цель работы
Реализовать численные методы решения нелинейных уравнений и сравнить их эффективность на примере функции:

$$f(x) = x^3 - 2x - 5 = 0$$

---

## 📁 Структура проекта

```
Lab4_Nonlinear_Equations/
├── CMakeLists.txt              # настройки сборки
├── include/
│   ├── methods.hpp             # объявления функций и структур
│   └── utils.hpp               # вспомогательные функции вывода
├── src/
│   ├── main.cpp                # точка входа
│   └── methods.cpp             # реализации методов
└── build/                      # создаётся автоматически при сборке
```

---

## ⚙️ Установка и сборка

### Linux / macOS / PowerShell
```bash
# Клонируем или создаём проект
mkdir -p Lab4_Nonlinear_Equations/{src,include,build}
cd Lab4_Nonlinear_Equations

# (скопируйте сюда файлы из проекта)

# Сборка
cd build
cmake ..
cmake --build .

# Запуск
./lab4
```

### Windows CMD
```cmd
mkdir Lab4_Nonlinear_Equations
cd Lab4_Nonlinear_Equations
mkdir src
mkdir include
mkdir build
cd build
cmake ..
cmake --build .
lab4.exe
```

---

## 🧩 Методы, реализованные в программе

### 1. Метод половинного деления (bisection)
- Требует, чтобы f(a) и f(b) имели разные знаки.
- Критерий останова: `(b - a)/2 < eps` или `|f(c)| < eps`.

### 2. Метод простой итерации (релаксация)
- Формула итерации: `x_{n+1} = x_n - λ * f(x_n)`.
- Коэффициент релаксации: `λ = 0.1`.
- Останавливается при `|x_{n+1} - x_n| < eps` или `|f(x)| < eps`.

### 3. Метод Ньютона
- Итерационная формула: `x_{n+1} = x_n - f(x_n)/f'(x_n)`.
- Ускоренно сходится при хорошем начальном приближении.

---

## 🧠 Используемые функции
- `f(x) = x^3 - 2x - 5`
- `f'(x) = 3x^2 - 2`

---

## 📊 Пример вывода программы
```
=== Bisection method (eps=1e-06) ===
 iter                x              f(x)         delta
------------------------------------------------------------
  ...
result: root=2.0945514812, iterations=21, |f|=0.0000000001
```

---

## 📈 Результаты
| Метод | Точность (eps) | Корень | Кол-во итераций |
|--------|----------------|---------|----------------|
| Bisection | 1e-6 | ≈2.09455148 | ~21 |
| Simple Iteration | 1e-6 | ≈2.09455148 | ~40–100 |
| Newton | 1e-6 | ≈2.09455148 | ~5–7 |

---

## 🧾 Примечания
- Для метода простой итерации можно подобрать оптимальное значение λ для ускорения сходимости.
- Если изменить функцию `f(x)` и её производную `df(x)` в `methods.cpp`, проект можно использовать для любых уравнений.

---

## 👨‍💻 Автор
Разработано как лабораторная работа по численным методам (C++).
